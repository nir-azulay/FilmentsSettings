fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
python-multipart>=0.0.9
# httpx is used to call the Home Assistant Core REST API via Supervisor
# (http://supervisor/core/api) for read-only AMS tray state. Required by
# the /api/ams/trays endpoint added in add-on 0.5.0.
httpx>=0.27.0
# Label generation (0.15.0)
qrcode[pil]>=7.4
Pillow>=10.0
# Niimbot B21 Pro BLE printing (0.15.0)
bleak>=0.21
bleak-retry-connector>=3.0

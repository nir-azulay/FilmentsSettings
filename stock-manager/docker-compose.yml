services:
  backend:
    build: ./backend
    volumes:
      - ./backend/data:/app/data
      - ../:/profiles:ro
    environment:
      - PROFILES_ROOT=/profiles

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
